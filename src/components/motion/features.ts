// Split point for LazyMotion. Kept in its own module so the feature bundle
// resolves as a separate chunk instead of joining the layout's.
import { domAnimation } from 'motion/react'

export default domAnimation
