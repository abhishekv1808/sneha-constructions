.next
node_modules
pnpm-lock.yaml

# The @theme block is transcribed verbatim from CLAUDE.md §5.1 and its column
# alignment is checked against that spec during review; Prettier would reflow it.
src/app/globals.css

# The brief is the source of truth for the design tokens. Prettier lowercases the
# hex values inside its code fences, which breaks a character-level diff against
# globals.css.
CLAUDE.md

# Generated by `pnpm db:types`. Prettier would reflow the generator output and
# make every regeneration produce a spurious diff.
src/lib/supabase/types.ts
